"""Your very first red/green loop.

Implement `greet` below, save the file, then run the tests. They start
red (failing); once `greet` is correct, they turn green.

Run: uv run pytest 01-setup-tooling -k ex01
"""


def greet(name):
    """Return a friendly greeting for `name`.

    Params:
        name (str): the person to greet.

    Returns:
        str: "Hello, <name>!".

    Examples:
        greet("Ada") -> "Hello, Ada!"
        greet("Grace") -> "Hello, Grace!"
    """
    raise NotImplementedError
